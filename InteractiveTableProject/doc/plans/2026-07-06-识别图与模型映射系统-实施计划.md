# 识别图与模型映射系统 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立「识别图 SymbolID → 3D 模型」配置表驱动的映射系统，首条映射 id=0 → Model_A（景泰蓝），实现方块移动时大屏模型跟随。

**Architecture:** 四层解耦——MarkerContentLibrary（ScriptableObject 配置表）、MarkerModelManager（TUIO 事件调度）、TrackedModelController（模型表现）、TuioWorldMapper（坐标数学）。管理器监听 Tuio11Dispatcher 的 Object Add/Remove 事件，查表实例化 Prefab 并驱动。

**Tech Stack:** Unity 2022.3.8f1 · TuioUnityClient 2.0.4（TuioNet.dll 已自动引用，Assets 脚本可直接用）· coplay MCP 操作编辑器 · reacTIVision 1.5.1 + 内置摄像头（DirectShow index 0）真机测试。

**测试策略（对 TDD 的偏离说明）:** 按已批准的设计文档，验证方式为真实环境端到端测试（reacTIVision + 打印识别图）。项目无单元测试基础设施，且核心逻辑强依赖 Unity 场景与外部硬件事件流；每个脚本任务以「MCP check_compile_errors 零错误」为通过门槛，最终以真机 E2E 三项行为验收。

**关键环境事实（执行者必读）:**
- 场景：`Assets/TUIO 1.1 Example Scene.unity`（编辑器当前打开，含未保存的 model_A）。
- 场景对象：`Tuio Session`（TuioSessionBehaviour）、`Main Camera`（正交，size 5，pos (0,1,-10)）、`model_A`（pos (0,1.509,3.893)，rot X270，scale 0.03，子物体 Polygonal_Model_1_Triangles_1）。
- Tuio Session 必须是 UDP / 127.0.0.1 / 3333 / Tuio11（历史坑：默认可能是 Websocket/10.0.0.20；coplay set_property 可能不落盘，改完必须 save_scene 并 grep 场景文件验证）。
- MCP 已 set_unity_project_root 到本项目；本机常开多个 Unity 实例，若 MCP 报多实例错误需重新 set_unity_project_root。
- API 事实：`Tuio11Object.Position` 是 System.Numerics.Vector2（大写 `.X/.Y`）；`SymbolId`、`SessionId` 可用，SessionId 为 uint；Dispatcher 事件为 `OnObjectAdd/OnObjectRemove`，签名 `EventHandler<Tuio11Object>`；获取 Dispatcher：`(Tuio11Dispatcher)_tuioSession.TuioDispatcher`（需 try/catch InvalidCastException，参照包内 Tuio11Visualizer.cs 的写法）。

---

### Task 1: 配置层 MarkerContentLibrary

**Files:**
- Create: `Assets/Scripts/TangibleTable/Config/MarkerContentLibrary.cs`

- [ ] **Step 1.1: 写入脚本**

```csharp
using System;
using System.Collections.Generic;
using UnityEngine;

namespace TangibleTable.Config
{
    public enum MarkerLostPolicy
    {
        KeepInPlace,    // 丢失后停在原地（默认，方块常驻桌面）
        Hide,           // 丢失立即隐藏
        HideAfterDelay  // 丢失超过 hideDelay 秒后隐藏
    }

    [Serializable]
    public class MarkerMapping
    {
        public int symbolId;
        public GameObject modelPrefab;
        public MarkerLostPolicy lostPolicy = MarkerLostPolicy.KeepInPlace;
        [Min(0f)] public float smoothTime = 0.1f;
        [Min(0f)] public float hideDelay = 1f;
    }

    [CreateAssetMenu(fileName = "MarkerContentLibrary", menuName = "TangibleTable/Marker Content Library")]
    public class MarkerContentLibrary : ScriptableObject
    {
        [SerializeField] private List<MarkerMapping> _mappings = new();

        public bool TryGetMapping(int symbolId, out MarkerMapping mapping)
        {
            foreach (var m in _mappings)
            {
                if (m.symbolId == symbolId)
                {
                    mapping = m;
                    return true;
                }
            }
            mapping = null;
            return false;
        }

#if UNITY_EDITOR
        private void OnValidate()
        {
            var seen = new HashSet<int>();
            foreach (var m in _mappings)
            {
                if (!seen.Add(m.symbolId))
                    Debug.LogWarning($"[MarkerContentLibrary] 重复的 symbolId: {m.symbolId}", this);
                if (m.modelPrefab == null)
                    Debug.LogWarning($"[MarkerContentLibrary] symbolId {m.symbolId} 未指定 modelPrefab", this);
            }
        }
#endif
    }
}
```

- [ ] **Step 1.2: 编译验证**

MCP `check_compile_errors`，期望：无错误。（若 MCP 报多 Unity 实例，先 `set_unity_project_root` 到本项目路径。）

- [ ] **Step 1.3: 提交**

```bash
git add "InteractiveTableProject/Assets/Scripts"
git commit -m "feat: 新增识别图映射配置表 MarkerContentLibrary"
```

---

### Task 2: 坐标映射 TuioWorldMapper

**Files:**
- Create: `Assets/Scripts/TangibleTable/Core/TuioWorldMapper.cs`

- [ ] **Step 2.1: 写入脚本**

```csharp
using UnityEngine;

namespace TangibleTable.Core
{
    /// <summary>
    /// TUIO 归一化坐标(0~1, y 向下) 与 Unity 世界坐标之间的换算。
    /// 桌面边缘对应相机视口边缘；depth 为沿相机朝向的距离，用于保持模型原有深度。
    /// </summary>
    public static class TuioWorldMapper
    {
        public static Vector3 ToWorld(Camera camera, Vector2 tuioPosition, float depth)
        {
            var viewport = new Vector3(tuioPosition.x, 1f - tuioPosition.y, depth);
            return camera.ViewportToWorldPoint(viewport);
        }

        public static float DepthOf(Camera camera, Vector3 worldPosition)
        {
            return Vector3.Dot(worldPosition - camera.transform.position, camera.transform.forward);
        }
    }
}
```

- [ ] **Step 2.2: 编译验证**

MCP `check_compile_errors`，期望：无错误。

- [ ] **Step 2.3: 提交**

```bash
git add "InteractiveTableProject/Assets/Scripts"
git commit -m "feat: 新增 TUIO 归一化坐标到世界坐标的映射工具"
```

---

### Task 3: 表现层 TrackedModelController

**Files:**
- Create: `Assets/Scripts/TangibleTable/Presentation/TrackedModelController.cs`

- [ ] **Step 3.1: 写入脚本**

```csharp
using TangibleTable.Config;
using TangibleTable.Core;
using TuioNet.Tuio11;
using UnityEngine;

namespace TangibleTable.Presentation
{
    /// <summary>
    /// 表现层：让模型跟随一个 TUIO Object。由 MarkerModelManager 在实例化 Prefab 时挂载并驱动，
    /// Prefab 本身无需预挂任何脚本。
    /// </summary>
    public class TrackedModelController : MonoBehaviour
    {
        private Tuio11Object _tuioObject;
        private MarkerMapping _mapping;
        private Camera _camera;
        private float _depth;
        private Vector3 _velocity;
        private float _lostTime;
        private bool _isTracking;

        public void Initialize(MarkerMapping mapping, Camera camera)
        {
            _mapping = mapping;
            _camera = camera;
            _depth = TuioWorldMapper.DepthOf(camera, transform.position);
        }

        public void OnMarkerFound(Tuio11Object tuioObject)
        {
            _tuioObject = tuioObject;
            _isTracking = true;
            gameObject.SetActive(true);
        }

        public void OnMarkerLost()
        {
            _tuioObject = null;
            _isTracking = false;
            _lostTime = Time.time;
            if (_mapping.lostPolicy == MarkerLostPolicy.Hide)
                gameObject.SetActive(false);
        }

        private void Update()
        {
            if (_isTracking)
            {
                var tuioPos = new Vector2(_tuioObject.Position.X, _tuioObject.Position.Y);
                var target = TuioWorldMapper.ToWorld(_camera, tuioPos, _depth);
                transform.position = Vector3.SmoothDamp(
                    transform.position, target, ref _velocity, _mapping.smoothTime);
            }
            else if (_mapping != null
                     && _mapping.lostPolicy == MarkerLostPolicy.HideAfterDelay
                     && Time.time - _lostTime > _mapping.hideDelay)
            {
                gameObject.SetActive(false);
            }
        }
    }
}
```

- [ ] **Step 3.2: 编译验证**

MCP `check_compile_errors`，期望：无错误。

- [ ] **Step 3.3: 提交**

```bash
git add "InteractiveTableProject/Assets/Scripts"
git commit -m "feat: 新增模型跟随控制器 TrackedModelController"
```

---

### Task 4: 调度层 MarkerModelManager

**Files:**
- Create: `Assets/Scripts/TangibleTable/Core/MarkerModelManager.cs`

- [ ] **Step 4.1: 写入脚本**

```csharp
using System;
using System.Collections.Generic;
using TangibleTable.Config;
using TangibleTable.Presentation;
using TuioNet.Tuio11;
using TuioUnity.Common;
using TuioUnity.Tuio11;
using UnityEngine;

namespace TangibleTable.Core
{
    /// <summary>
    /// 输入/调度层：监听 TUIO 1.1 Object 的 Add/Remove 事件，
    /// 按 SymbolID 查 MarkerContentLibrary，实例化并驱动对应模型。
    /// </summary>
    public class MarkerModelManager : MonoBehaviour
    {
        [SerializeField] private TuioSessionBehaviour _tuioSession;
        [SerializeField] private MarkerContentLibrary _library;
        [SerializeField] private Camera _targetCamera;

        private readonly Dictionary<int, TrackedModelController> _controllersBySymbol = new();
        private readonly Dictionary<uint, int> _symbolBySession = new();

        private Tuio11Dispatcher Dispatcher => (Tuio11Dispatcher)_tuioSession.TuioDispatcher;

        private void Awake()
        {
            if (_targetCamera == null) _targetCamera = Camera.main;
        }

        private void OnEnable()
        {
            try
            {
                Dispatcher.OnObjectAdd += HandleObjectAdd;
                Dispatcher.OnObjectRemove += HandleObjectRemove;
            }
            catch (InvalidCastException exception)
            {
                Debug.LogError($"[MarkerModelManager] 请检查 Tuio Session 的 TUIO 版本是否为 1.1。{exception.Message}");
            }
        }

        private void OnDisable()
        {
            try
            {
                Dispatcher.OnObjectAdd -= HandleObjectAdd;
                Dispatcher.OnObjectRemove -= HandleObjectRemove;
            }
            catch (InvalidCastException exception)
            {
                Debug.LogError($"[MarkerModelManager] 请检查 Tuio Session 的 TUIO 版本是否为 1.1。{exception.Message}");
            }
        }

        private void HandleObjectAdd(object sender, Tuio11Object tuioObject)
        {
            var symbolId = (int)tuioObject.SymbolId;
            if (!_library.TryGetMapping(symbolId, out var mapping))
            {
                Debug.Log($"[MarkerModelManager] 未配置的识别图 ID: {symbolId}，忽略。");
                return;
            }

            if (_symbolBySession.ContainsValue(symbolId))
            {
                Debug.LogWarning($"[MarkerModelManager] 识别图 ID {symbolId} 已在追踪中，忽略重复放置。");
                return;
            }

            if (!_controllersBySymbol.TryGetValue(symbolId, out var controller))
            {
                var instance = Instantiate(mapping.modelPrefab);
                instance.name = $"{mapping.modelPrefab.name} [id={symbolId}]";
                controller = instance.AddComponent<TrackedModelController>();
                controller.Initialize(mapping, _targetCamera);
                _controllersBySymbol.Add(symbolId, controller);
            }

            _symbolBySession[tuioObject.SessionId] = symbolId;
            controller.OnMarkerFound(tuioObject);
        }

        private void HandleObjectRemove(object sender, Tuio11Object tuioObject)
        {
            if (_symbolBySession.Remove(tuioObject.SessionId, out var symbolId)
                && _controllersBySymbol.TryGetValue(symbolId, out var controller))
            {
                controller.OnMarkerLost();
            }
        }
    }
}
```

- [ ] **Step 4.2: 编译验证**

MCP `check_compile_errors`，期望：无错误。若 `SymbolId`/`SessionId` 类型报错，以编译器提示的实际类型调整字典键类型（设计不变）。

- [ ] **Step 4.3: 提交**

```bash
git add "InteractiveTableProject/Assets/Scripts"
git commit -m "feat: 新增 TUIO 事件调度器 MarkerModelManager"
```

---

### Task 5: 资产与场景装配（通过 coplay MCP）

**Files:**
- Create: `Assets/Prefabs/Model_A.prefab`（由场景中 model_A 固化）
- Create: `Assets/Settings/MarkerContentLibrary.asset`（首条映射 id=0）
- Modify: `Assets/TUIO 1.1 Example Scene.unity`（新增 Marker Model Manager，移除散放的 model_A，落盘未保存的更改）

- [ ] **Step 5.1: model_A 固化为 Prefab**

用 MCP `create_prefab`（或 `execute_script` 调 `PrefabUtility.SaveAsPrefabAsset`）把场景中 `model_A` 存为 `Assets/Prefabs/Model_A.prefab`，保留其 transform（pos (0,1.509,3.893) / rot X270 / scale 0.03）。Prefab 根节点的位置即实例化后的初始位置，其 Z 决定跟随深度，勿归零。

- [ ] **Step 5.2: 删除场景中的 model_A**

MCP `delete_game_object`（路径 `model_A`）。模型今后由管理器按需实例化。

- [ ] **Step 5.3: 创建配置表资产并填入首条映射**

MCP `execute_script` 执行编辑器代码：创建 `MarkerContentLibrary` 实例存到 `Assets/Settings/MarkerContentLibrary.asset`，添加条目 { symbolId=0, modelPrefab=Model_A.prefab, lostPolicy=KeepInPlace, smoothTime=0.1 }，`EditorUtility.SetDirty` + `AssetDatabase.SaveAssets`。（\_mappings 为私有序列化字段，用 `SerializedObject` 或反射赋值均可。）

- [ ] **Step 5.4: 创建管理器对象并接线**

MCP `create_game_object` 名为 `Marker Model Manager`，`add_component` MarkerModelManager，`set_property` 三个引用：_tuioSession → 场景 `Tuio Session`、_library → 上一步资产、_targetCamera → `Main Camera`。

- [ ] **Step 5.5: 核对 Tuio Session 配置**

确认 TuioVersion=Tuio11 / ConnectionType=UDP / Address=127.0.0.1 / Port=3333（历史上默认值是 Websocket/10.0.0.20 的坑）。

- [ ] **Step 5.6: 保存场景并验证落盘**

MCP `save_scene`；然后在磁盘上 grep `Assets/TUIO 1.1 Example Scene.unity` 确认包含 `Marker Model Manager` 且不含 `model_A`（coplay set_property 有不落盘前科）。若未落盘，直接编辑场景 YAML 补齐后让编辑器刷新。

- [ ] **Step 5.7: 提交**

```bash
git add "InteractiveTableProject/Assets/Prefabs" "InteractiveTableProject/Assets/Settings" \
        "InteractiveTableProject/Assets/Model" "InteractiveTableProject/Assets/Model.meta" \
        "InteractiveTableProject/Assets/TUIO 1.1 Example Scene.unity"
git commit -m "feat: 装配识别图映射系统——Model_A prefab、配置表资产、场景管理器"
```

（Assets/Model 是景泰蓝源模型，Prefab 依赖它，一并入库。）

---

### Task 6: 真实环境端到端验收

**前置：** 需要用户在场（移动打印识别图）。reacTIVision exe 位置若未知，先问用户或在磁盘搜索 `reacTIVision*.exe`。

- [ ] **Step 6.1: 启动 reacTIVision**

后台启动 reacTIVision.exe（内置摄像头 DirectShow index 0）。确认其窗口显示摄像头画面。注意本机有 Clash TUN 代理，若 UDP 不通检查代理拦截。

- [ ] **Step 6.2: Unity 进入 Play 模式**

MCP `play_game`。请用户把识别图对准摄像头，观察 reacTIVision 窗口显示的 ID 数字。

- [ ] **Step 6.3: 三项行为验收**

1. 识别图入镜 → 大屏出现景泰蓝模型并跟随移动；
2. 移动过程平滑无抖动（smoothTime=0.1）；
3. 遮挡识别图再露出 → 模型停在原地，随后恢复跟随。

用 MCP `get_unity_logs` 佐证（应看到 Add 事件、无报错；若识别图实际 ID≠0，日志会出现「未配置的识别图 ID: N」——此时把配置表 symbolId 改为 N，重测）。

- [ ] **Step 6.4: 停止 Play、收尾提交**

MCP `stop_game`。若验收中调整过配置表，提交：

```bash
git add "InteractiveTableProject/Assets/Settings"
git commit -m "fix: 按实测识别图 ID 校正映射表"
```

---

## 完成定义

- 四个脚本编译零错误；
- 场景装配落盘（git diff 可见）；
- 真机 E2E 三项行为全部通过并有日志佐证；
- 全部变更已分任务提交。
